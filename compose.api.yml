services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: country-compare-backend:latest
    environment:
      COUNTRY_COMPARE_API_ENABLE_DOCS: ${COUNTRY_COMPARE_API_ENABLE_DOCS:-false}
      COUNTRY_COMPARE_API_CORS_ORIGINS: ${COUNTRY_COMPARE_API_CORS_ORIGINS:-}
      COUNTRY_COMPARE_API_MAX_RECORDS: ${COUNTRY_COMPARE_API_MAX_RECORDS:-500}
      COUNTRY_COMPARE_API_MAX_COUNTRIES: ${COUNTRY_COMPARE_API_MAX_COUNTRIES:-50}
      COUNTRY_COMPARE_API_MAX_METRICS: ${COUNTRY_COMPARE_API_MAX_METRICS:-50}
      COUNTRY_COMPARE_API_MAX_HORIZON_YEARS: ${COUNTRY_COMPARE_API_MAX_HORIZON_YEARS:-10}
      COUNTRY_COMPARE_API_MAX_HOLDOUT_YEARS: ${COUNTRY_COMPARE_API_MAX_HOLDOUT_YEARS:-10}
      COUNTRY_COMPARE_API_MAX_TOP_N: ${COUNTRY_COMPARE_API_MAX_TOP_N:-100}
      COUNTRY_COMPARE_API_KEY: ${COUNTRY_COMPARE_API_KEY:-}
      COUNTRY_COMPARE_API_LOG_LEVEL: ${COUNTRY_COMPARE_API_LOG_LEVEL:-INFO}
      COUNTRY_COMPARE_STORE_BACKEND: ${COUNTRY_COMPARE_STORE_BACKEND:-parquet}
      PORT: ${PORT:-8000}
      OMP_NUM_THREADS: ${OMP_NUM_THREADS:-1}
      OPENBLAS_NUM_THREADS: ${OPENBLAS_NUM_THREADS:-1}
      MKL_NUM_THREADS: ${MKL_NUM_THREADS:-1}
      NUMEXPR_NUM_THREADS: ${NUMEXPR_NUM_THREADS:-1}
      COUNTRY_COMPARE_STORE_PATH: ${COUNTRY_COMPARE_STORE_PATH:-/app/data/processed/metrics.parquet}
      COUNTRY_COMPARE_METRICS_CONFIG: ${COUNTRY_COMPARE_METRICS_CONFIG:-/app/config/metrics.yaml}
      COUNTRY_COMPARE_SCORING_CONFIG: ${COUNTRY_COMPARE_SCORING_CONFIG:-/app/config/scoring_profiles.yaml}
      COUNTRY_COMPARE_AUDIT_DIR: ${COUNTRY_COMPARE_AUDIT_DIR:-/app/data/audit}
      COUNTRY_COMPARE_EXPORT_DIR: ${COUNTRY_COMPARE_EXPORT_DIR:-/app/data/exports}

      COUNTRY_COMPARE_API_ENV: ${COUNTRY_COMPARE_API_ENV:-production}
      COUNTRY_COMPARE_API_AUTH_REQUIRED: ${COUNTRY_COMPARE_API_AUTH_REQUIRED:-true}
      
      COUNTRY_COMPARE_API_PROTECT_DOCS: ${COUNTRY_COMPARE_API_PROTECT_DOCS:-true}
      
      COUNTRY_COMPARE_API_ENABLE_METRICS: ${COUNTRY_COMPARE_API_ENABLE_METRICS:-true}
      COUNTRY_COMPARE_API_PROTECT_METRICS: ${COUNTRY_COMPARE_API_PROTECT_METRICS:-true}
      
      COUNTRY_COMPARE_API_LOG_FORMAT: ${COUNTRY_COMPARE_API_LOG_FORMAT:-json}
      COUNTRY_COMPARE_API_CONFIGURE_LOGGING: ${COUNTRY_COMPARE_API_CONFIGURE_LOGGING:-true}
      COUNTRY_COMPARE_API_LOG_PROPAGATE: ${COUNTRY_COMPARE_API_LOG_PROPAGATE:-true}
      
      COUNTRY_COMPARE_API_PROTECTED_PREFIXES: ${COUNTRY_COMPARE_API_PROTECTED_PREFIXES:-/api/v1,/ready}
      COUNTRY_COMPARE_API_PUBLIC_PATHS: ${COUNTRY_COMPARE_API_PUBLIC_PATHS:-/health}
      
      COUNTRY_COMPARE_CONFIG_DIR: ${COUNTRY_COMPARE_CONFIG_DIR:-/app/config}
      COUNTRY_COMPARE_DATA_DIR: ${COUNTRY_COMPARE_DATA_DIR:-/app/data}

      COUNTRY_COMPARE_ENABLE_LLM_FORECAST: ${COUNTRY_COMPARE_ENABLE_LLM_FORECAST:-true}
      COUNTRY_COMPARE_LLM_SERVICE_URL: ${COUNTRY_COMPARE_LLM_SERVICE_URL:-http://llm-forecast:8080}
      COUNTRY_COMPARE_LLM_SERVICE_TOKEN: ${COUNTRY_COMPARE_LLM_SERVICE_TOKEN:-}
      COUNTRY_COMPARE_LLM_SERVICE_TIMEOUT_SECONDS: ${COUNTRY_COMPARE_LLM_SERVICE_TIMEOUT_SECONDS:-75}
      COUNTRY_COMPARE_LLM_MAX_ADJUSTMENT_PCT: ${COUNTRY_COMPARE_LLM_MAX_ADJUSTMENT_PCT:-15.0}
    volumes:
      - ./config:/app/config:ro
      - ./data/processed:/app/data/processed:ro
    ports:
      - "${COUNTRY_COMPARE_API_PORT:-8000}:${PORT:-8000}"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - |
          import urllib.request
          import os
          port = os.environ.get('PORT', '8000')
          urllib.request.urlopen(f'http://127.0.0.1:{port}/health', timeout=5).read()
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s